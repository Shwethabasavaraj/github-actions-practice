name: Where is my code?

on: [push]

jobs:
  show-path:
    runs-on: ubuntu-latest

  steps: 
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Show working directory
      run: pwd

    - name: List files
      run: ls -la
